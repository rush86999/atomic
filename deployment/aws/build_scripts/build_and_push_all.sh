#!/bin/bash
set -e

# This script is not needed for fly.io deployments.
# The `flyctl deploy` command will build and push the images automatically.

echo "This script is not needed for fly.io deployments."
exit 0
