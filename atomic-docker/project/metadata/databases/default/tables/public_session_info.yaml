table:
  name: session_info
  schema: public
object_relationships:
  - name: tenant
    using:
      manual_configuration:
        column_mapping:
          app_id: app_id
          tenant_id: tenant_id
        insertion_order: null
        remote_table:
          name: tenants
          schema: public
