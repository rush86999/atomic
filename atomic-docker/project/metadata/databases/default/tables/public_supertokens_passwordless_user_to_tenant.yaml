table:
  name: supertokens_passwordless_user_to_tenant
  schema: public
object_relationships:
  - name: supertokens_all_auth_recipe_user
    using:
      manual_configuration:
        column_mapping:
          app_id: app_id
          tenant_id: tenant_id
          user_id: user_id
        insertion_order: null
        remote_table:
          name: supertokens_all_auth_recipe_users
          schema: public
