# This is the roles mapping configuration file
# Role mappings define which users or backend roles are mapped to which roles

all_access:
  reserved: true
  backend_roles:
    - "admin"
  users:
    - "admin"

read_only:
  reserved: false
  backend_roles:
    - "read_only"
  users:
    - "user1"
