

export type CustomerType = {
    id: string,
    name?: string,
    email?: string,
    phone?: number,
    userId: string,
    description?: string,
    address?: string,
    createdDate?: string,
    updatedAt?: string,
}