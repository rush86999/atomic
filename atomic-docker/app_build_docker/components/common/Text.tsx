import {
  createRestyleComponent,
  createText, createVariant, VariantProps,
} from '@shopify/restyle';
import {Theme} from '@lib/theme/theme';


const Text = createText<Theme>();

export default Text;
