from setuptools import setup, find_packages

setup(
    name='atom-project',
    version='0.1.0',
    packages=find_packages(),
    description='A collection of packages for the Atom project.',
    author='Atom Team',
    author_email='dev@atom.com',
)
