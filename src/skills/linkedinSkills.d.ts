import { SkillResponse } from '../../atomic-docker/project/functions/atom-agent/types';
export declare function scheduleLinkedInPost(userId: string, content: string, sendAt: string): Promise<SkillResponse<any>>;
